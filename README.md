#  Stage 0  System Foundations (Linux & Windows Hardening)

This project demonstrates
- Linux user and group management
- Log inspection with `tail`, `grep`, and `journalctl`
- SSH and password hardening using `sshd_config`, `chage`, and Fail2Ban
- Windows Defender, Firewall, and Event Viewer configuration
